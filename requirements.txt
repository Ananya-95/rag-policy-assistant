# Core
python-dotenv>=1.0.0

# LangChain stack (versions loosely pinned; adjust for your environment)
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0
langchain-huggingface>=0.0.3
langchain-groq>=0.1.0
langchain-experimental>=0.0.50
langchain-text-splitters>=0.2.0

# PDFs & vector search
pypdf>=4.0.0
faiss-cpu>=1.7.4

# Local embeddings (downloads model weights on first use)
sentence-transformers>=2.2.0
